r#equire 'spec_helper'

#on_supported_os.each do |os, os_facts|
  #context "on #{os}" do
    #let(:facts) { os_facts }
  
    #if facts[:osfamily] == 'Ubuntu'

    #describe 'profile::beats::winlogbeat' do
      #it { is_expected.to contain_class('winlogbeat') }
    #end

    #describe 'winlogbeat' do
      

